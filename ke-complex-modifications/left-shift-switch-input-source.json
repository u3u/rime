{
  "rules": [
    {
      "description": "Post f17 if left_shift is pressed alone.",
      "manipulators": [
        {
          "from": { "key_code": "left_shift", "modifiers": { "optional": ["any"] } },
          "to": [{ "key_code": "left_shift" }],
          "to_if_alone": [{ "key_code": "f17" }],
          "type": "basic"
        }
      ]
    }
  ],
  "title": "Left shift switch input source"
}
